<%= form_for event, remote: true do |f| %>
  <%= f.check_box :complete %>
  <%= f.label :complete, event.name %>
  <%= link_to "(remove)", event, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>